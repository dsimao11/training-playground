pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
	environment { 
        NOTIFICATION_EMAIL = 'clang'
    }
    stages {
        stage('build') {
            steps {
                echo "$NOTIFICATION_EMAIL"
            }
        }
    }
}
